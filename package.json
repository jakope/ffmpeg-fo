{
  "name": "@jakope/ffmpeg-fo",
  "version": "0.1.31",
  "description": "",
  "private": false,
  "main": "src/ffmpeg-command-builder.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pub": "yarn version --patch && git push && npm publish --access public",
    "publish:only": "yarn publish --access public",
    "lint": "eslint ."
  },
  "author": "",
  "type": "module",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.52.0",
    "eslint-plugin-prettier": "^5.0.1"
  }
}
